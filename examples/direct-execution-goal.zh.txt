决策摘要：任务类型=docs；成熟度=已有仓库；外部信息需求=不需要；风险等级=低；输出长度=短版；是否先提问=否
默认假设：目标是小范围文档和脚本维护，仓库已有 README、manifest 和校验脚本；不需要联网研究、Agent Reach 或浏览器自动化。
偏好应用：这是低风险维护任务，优先快速闭环到 README、校验脚本和示例一致性；不强行加入联网调研或增长分析。
反馈调整：未提供上次执行反馈，本次不做反馈修正；如果执行后发现校验范围不足，下次再补坏例子和 CI 检查。
优先级判断：业务价值=中；证据强度=高；执行成本=低；分发潜力=中；变现路径=提升 skill 安装和使用可靠性；风险=改动范围扩散；建议=直接执行短版任务，不做研究流程。
输出长度：短版；只给聚焦的直接执行 goal。
选择理由：这是明确的本地仓库维护任务，直接执行比三阶段研究更合适；选择短版可以避免把简单改动复杂化。
推荐执行版（中文，可直接复制）
/goal 基于当前项目上下文完善 README 顶部主题列表和安装更新说明，先读取已有 README、manifest、示例和维护脚本，再做最小必要改动。
验证方式：运行 `python3 -m json.tool manifest.json`、`python3 scripts/check_readme_topics.py`、`python3 -m py_compile scripts/validate_xiaowei_goal.py scripts/check_readme_topics.py`，并用命令输出证明检查通过。
限制：不改核心 skill 定位，不改公开接口，不加入账号、支付、部署、生产配置、凭证或复杂新功能。
工作边界：只修改 README、manifest、校验脚本和必要的示例索引，不移动历史文档，不删除已有页面。
推进规则：一次只做一个聚焦改动；每次有意义改动后重跑相关检查；失败时先读错误和脚本，再调整方案。
停止标准：README 顶部主题列表和 manifest 元数据一致，安装更新说明清楚，相关检查通过。
暂停条件：需要删除历史页面、改 GitHub Pages 发布方式、使用凭证、访问付费服务、处理生产数据或做破坏性操作时暂停。
