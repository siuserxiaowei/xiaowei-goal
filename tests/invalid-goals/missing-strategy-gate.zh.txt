决策摘要：任务类型=docs；成熟度=已有仓库；外部信息需求=不需要；风险等级=低；输出长度=短版；是否先提问=否
默认假设：目标是小范围文档维护，仓库已有 README 和校验脚本；不需要联网研究或浏览器自动化。
偏好应用：优先做小闭环，直接修文档和校验，不扩大成研究任务。
反馈调整：未提供上次执行反馈，本次不做反馈修正。
优先级判断：业务价值=中；证据强度=高；执行成本=低；分发潜力=中；变现路径=提升使用可靠性；风险=改动范围扩散；建议=直接执行。
输出长度：短版。
选择理由：这是明确的小维护任务，直接执行比三阶段研究更合适。
推荐执行版（中文，可直接复制）
/goal 基于当前项目上下文完善 README 安装说明，先读取已有 README 和脚本，再做最小必要改动。
验证方式：运行 `python3 -m json.tool manifest.json` 和 `python3 scripts/check_readme_topics.py`，用命令输出证明检查通过。
限制：不改核心 skill 定位，不加入账号、支付、部署、生产配置、凭证或复杂新功能。
工作边界：只修改 README 和必要的校验脚本。
推进规则：小步修改，重跑检查，失败时先读错误再调整。
停止标准：README 更新完成且相关检查通过。
暂停条件：需要凭证、生产数据、破坏性操作或大范围重构时暂停。
