决策摘要：任务类型=skill-evolution；成熟度=已有仓库；外部信息需求=不需要；风险等级=中；输出长度=标准版；是否先提问=否
默认假设：目标仓库是 xiaowei-goal，用户希望每天自动进化一次。
偏好应用：优先做小闭环自动进化，不做无边界后台自治。
反馈调整：未提供上次执行反馈，本次不做反馈修正。
优先级判断：业务价值=高；证据强度=中；执行成本=中；分发潜力=中；变现路径=提升 skill 可靠性；风险=后台任务乱改仓库；建议=立即做。
输出长度：标准版；给出每日自动进化目标。
选择理由：这是明确的 skill 自我维护任务，应该自动运行。
推荐执行版（中文，可直接复制）
/goal 让 xiaowei-goal 每天自动进化一次：自动检查反馈、修改仓库、推送并发布。
自动化边界：用户显式要求后启用每日自动进化；不是跨仓库后台自治。
反馈来源：用户反馈、examples、validator 和 CI 日志。
允许修改路径：仅限 `SKILL.md`、`README.md`、`manifest.json`、`references/`、`examples/`、`scripts/`、`tests/`。
评估方式：运行 validator 和目标评估脚本。
验证方式：运行 `python3 scripts/validate_xiaowei_goal.py examples/*.txt` 和 `python3 scripts/test_validator_negative_cases.py`。
发布规则：本地通过后推送并发布。
回滚方式：出问题就修复。
限制：不提交凭证或生产数据。
工作边界：只改当前仓库的允许路径，不碰本地安装副本、生产数据、私域内容或无关仓库。
推进规则：每天运行一次，先检查反馈和校验结果，再决定是否需要修改、推送和发布。
停止标准：每日进化完成，校验通过，必要的问题已经记录，且没有留下临时目录。
暂停条件：需要凭证、生产数据或破坏性操作时暂停。
