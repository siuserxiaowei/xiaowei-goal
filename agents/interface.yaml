interface:
  display_name: "Xiaowei Goal"
  short_description: "Turn app, website, growth, SEO, competitor, and vague business tasks into three-stage research /goal commands."
  default_prompt: "Use $xiaowei-goal to turn this task into a Xiaowei-style executable /goal. For app, website, SaaS, landing page, SEO, growth, product, competitor, or search-first tasks, create a three-stage goal: broad research, Deep Research, then business application. Prefer Agent Reach or available search/browser tools, define source count, separate facts from inferences, and turn all collected, deduplicated, relevant, source-backed material into business actions before implementation. Use Chinese first when the user writes Chinese."
compatibility:
  canonical_format: "agent-skills"
  adapter_targets:
    - "openai"
    - "claude"
    - "codex"
    - "generic"
  activation:
    mode: "manual"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "public"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    openai: "drafts three-stage research Codex /goal commands from available task context"
    claude: "drafts structured agent goals with broad research, Deep Research, business application, and execution guardrails"
    codex: "drafts durable /goal commands with evidence, boundaries, and pause conditions"
    generic: "uses the Xiaowei goal contract and asks for missing high-impact decisions"
